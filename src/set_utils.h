#ifndef SET_UTILS_H
#define SET_UTILS_H

#include "vector_utils.h"
#include "types.h"

int check_duplicated_set(vector<efficient_set>,
                         evaluated_point,
                         double);

#endif
