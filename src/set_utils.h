#ifndef SET_UTILS_H
#define SET_UTILS_H

#include "vector_utils.h"

int check_duplicated_set(std::vector<efficient_set>,
                         evaluated_point,
                         double);

#endif
